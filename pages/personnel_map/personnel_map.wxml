<view>
	<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="16" show-location style="width: 100%; height: 100vh;"
	markers="{{markers}}"
	enable-zoom
	enable-scroll
	bindmarkertap="click"
	
	>
	</map>
</view>